# Скопируй в frontend/.env и заполни значения
# Используется в docker-compose.yml (локальная разработка)
#
# В проде VITE_API_BASE_URL задаётся через build arg в docker-compose.prod.yml

VITE_API_BASE_URL=/api/v1
